/**
 * Служебные подписи сцены, которая идёт сама.
 *
 * ЗАЧЕМ ОТДЕЛЬНЫЙ ФАЙЛ. Эти две строки принадлежат не префрейму и не подъёму в
 * Conversion District, а самому механизму тактов: они одинаковы во всех сценах
 * и обязаны оставаться одинаковыми. Держать их значениями по умолчанию прямо в
 * `SceneShell` было нельзя — копирайта в компонентах не бывает (docs/SPEC.md
 * §5.1), а разложить по трём файлам контента значило бы завести три места, где
 * можно случайно назвать одно и то же действие по-разному.
 */

export const sceneUi = {
  /**
   * Подсказка про тап.
   *
   * ФОРМУЛИРОВКА ВАЖНЕЕ, ЧЕМ КАЖЕТСЯ. Сцена идёт сама, и человек НЕ ОБЯЗАН
   * ничего нажимать — в этом вся разница с прежней воронкой, где после каждой
   * мысли стояло «дальше». Поэтому здесь не «нажми, чтобы продолжить»: такая
   * подпись превращает необязательный ускоритель в требование и возвращает
   * ровно ту механику, от которой ушли. Сказано, что тап ускоряет, а не что он
   * нужен.
   */
  tapHint: 'ТАП — БЫСТРЕЕ',

  /** Развернуть сцену целиком. Для тех, кто читает быстрее любого темпа. */
  skip: 'ПРОПУСТИТЬ',

  /**
   * Что слышит экранный диктор на области сцены. Для него подсказка про тап
   * бесполезна — он читает по-своему, — а вот назначение области нужно.
   */
  tapAria: 'Следующая реплика',
} as const;
