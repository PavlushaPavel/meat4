/**
 * Служебные подписи сцены, которая идёт сама.
 *
 * ЗАЧЕМ ОТДЕЛЬНЫЙ ФАЙЛ. Эти две строки принадлежат не префрейму и не подъёму в
 * Conversion District, а самому механизму тактов: они одинаковы во всех сценах
 * и обязаны оставаться одинаковыми. Держать их значениями по умолчанию прямо в
 * `SceneShell` было нельзя — копирайта в компонентах не бывает (docs/SPEC.md
 * §5.1), а разложить по трём файлам контента значило бы завести три места, где
 * можно случайно назвать одно и то же действие по-разному.
 */

export const sceneUi = {
  /**
   * Подсказка про тап.
   *
   * ФОРМУЛИРОВКА ВАЖНЕЕ, ЧЕМ КАЖЕТСЯ. Сцена идёт сама, и человек НЕ ОБЯЗАН
   * ничего нажимать — в этом вся разница с прежней воронкой, где после каждой
   * мысли стояло «дальше». Поэтому здесь не «нажми, чтобы продолжить»: такая
   * подпись превращает необязательный ускоритель в требование и возвращает
   * ровно ту механику, от которой ушли. Сказано, что тап ускоряет, а не что он
   * нужен.
   */
  tapHint: 'ТАП — БЫСТРЕЕ',

  /** Развернуть сцену целиком. Для тех, кто читает быстрее любого темпа. */
  skip: 'ПРОПУСТИТЬ',

  /**
   * Что слышит экранный диктор на области сцены. Для него подсказка про тап
   * бесполезна — он читает по-своему, — а вот назначение области нужно.
   */
  tapAria: 'Следующая реплика',
} as const;

/**
 * Голосовое сообщение, с которого начинается воронка.
 *
 * ЗАЧЕМ ОНО ВООБЩЕ. Текст вступления написан под произнесение — короткие фразы,
 * второе лицо, разговорный ритм. Прочитанный молча, он превращается в список
 * тезисов: человеку СООБЩАЮТ то, что должно было стать узнаванием. Голос
 * возвращает вступлению его настоящий носитель, а нажатие на воспроизведение
 * становится первым осмысленным жестом — тем самым, которым человек соглашается
 * слушать.
 *
 * ПОЧЕМУ ФОРМА МЕССЕНДЖЕРА. Ровно перед этим на экране лежит настоящая
 * переписка с клиентом. Голосовое приходит в неё же — не «плеер на сайте», а
 * сообщение от человека, и узнаваемость этой формы сильнее любой стилизации.
 */
export const voiceUi = {
  /** Кто прислал. Имя автора берётся из `src/content/author.ts`. */
  sender: 'Голосовое сообщение',

  /** Подпись под пузырём, пока человек не нажал. */
  invite: 'Нажми, чтобы послушать',

  /**
   * Записи ещё нет.
   *
   * Пузырь при этом НЕ притворяется играющим: показывать полосу, бегущую по
   * тишине, — то же самое, что выдуманный таймкод на несуществующем видео.
   * Кнопка честно ведёт дальше, и вступление идёт текстом, как раньше.
   */
  pending: 'Запись пока не подключена',
  pendingAction: 'Начать без записи',

  /** Что делает главная кнопка пузыря, когда запись есть. */
  play: 'Слушать',
  pause: 'Пауза',
  resume: 'Продолжить',

  /** Экранному диктору: полоса записи и переключатель скорости. */
  seekAria: 'Перемотка записи',
  rateAria: 'Скорость воспроизведения',
} as const;
